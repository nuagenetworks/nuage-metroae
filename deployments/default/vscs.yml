###############################################################################
# VSCs
#
# Nuage MetroAE VSC settings
#
# Automatically generated by conversion from /Users/mpiecuch/nuage-metro-tests/robot/resources/templates/kvm_sa/build_vars_kvm.yml.
#

#
# VSC 1
#
-
    # < Hostname >
    #
    hostname: "vsc1.example.met"

    # < VM name >
    # Used to identify VM on the hypervisor/vcenter.
    #
    vmname: "vsc1"

    # < Server type >
    # (kvm, vcenter, heat)
    #
    target_server_type: "kvm"

    # < Server hostname >
    #
    target_server: "127.0.0.1"

    # < Bridge interface >
    #
    # mgmt_bridge: (global Bridge interface)

    # < Management IP address >
    #
    mgmt_ip: "192.168.1.121"

    # < Management IP prefix length >
    #
    mgmt_ip_prefix: 24

    # < Management IP gateway >
    #
    mgmt_gateway: "192.168.1.1"

    # < VSD fqdn >
    #

    # < Routing IP address >
    # Only required when IGP or BGP routing will be configured
    #
    system_ip: "1.1.1.2"

    # < XMPP username >
    #
    xmpp_username: "vsc1"

    # < Static route IP prefixes >
    #
    mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    # < Control IP address >
    #
    ctrl_ip: "192.168.123.221"

    # < Control netmask prefix length >
    #
    ctrl_ip_prefix: 24


    # < KVM cpuset information >
    #
    # cpuset: [ 1, 2, 3, 4 ]




    # < Ejabberd user id >
    # Ejabberd user id used to create the certificate
    #
    #ejabberd_id: ""

    # < Private key path >
    # Path to VSC certificate key pem file
    #
    #private_key: ""

    # < Certificate path >
    # Path to VSC certificate pem file
    #
    #certificate_path: ""

    # < Root certificate path >
    # Path to Root certificate pem file
    #
    #ca_certificate_path: ""

    # < XMPP domain >
    # XMPP domain used in custom certificates
    #
    #xmpp_domain: ""


    # < Datacenter >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datacenter: (global VCenter datacenter)

    # < Cluster >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_cluster: (global VCenter cluster)

    # < Datastore >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datastore: (global VCenter datastore)

    # < Expected number BGP peers >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0


    credentials_set: ComponentCredentials




#
# VSC 2
#
-
    # < Hostname >
    #
    hostname: "vsc2.example.met"

    # < VM name >
    # Used to identify VM on the hypervisor/vcenter.
    #
    vmname: "vsc2"

    # < Server type >
    # (kvm, vcenter, heat)
    #
    target_server_type: "kvm"

    # < Server hostname >
    #
    target_server: "127.0.0.1"

    # < Bridge interface >
    #
    # mgmt_bridge: (global Bridge interface)

    # < Management IP address >
    #
    mgmt_ip: "192.168.1.122"

    # < Management IP prefix length >
    #
    mgmt_ip_prefix: 24

    # < Management IP gateway >
    #
    mgmt_gateway: "192.168.1.1"

    # < VSD fqdn >
    #

    # < Routing IP address >
    # Only required when IGP or BGP routing will be configured
    #
    system_ip: "1.1.1.3"

    # < XMPP username >
    #
    xmpp_username: "vsc2"

    # < Static route IP prefixes >
    #
    mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    # < Control IP address >
    #
    ctrl_ip: "192.168.123.222"

    # < Control netmask prefix length >
    #
    ctrl_ip_prefix: 24

    # < Datacenter >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datacenter: (global VCenter datacenter)

    # < Cluster >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_cluster: (global VCenter cluster)

    # < Datastore >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datastore: (global VCenter datastore)

    # < Expected number BGP peers >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

