###############################################################################
# VSDs
#
# Nuage MetroAE VSD settings
#
# Automatically generated by conversion from /Users/mpiecuch/nuage-metro-tests/robot/resources/templates/kvm_sa/build_vars_kvm.yml.
#

#
# VSD 1
#
-
    # < Hostname >
    #
    hostname: "vsd1.example.met"

    # < VM name >
    # Used to identify VM on the hypervisor/vcenter.
    #
    vmname: "vsd1"

    # < Upgrade VM name >
    #
    upgrade_vmname: "new-vsd1"

    # < Server type >
    # (kvm, vcenter, heat)
    #
    target_server_type: "kvm"

    # < Server hostname >
    #
    target_server: "127.0.0.1"

    # < Bridge interface >
    #
    # mgmt_bridge: (global Bridge interface)

    # < Management IP address >
    #
    mgmt_ip: "192.168.1.111"

    # < Management IP prefix length >
    #
    mgmt_ip_prefix: 24

    # < Management IP gateway >
    #
    mgmt_gateway: "192.168.1.1"

    # < Datacenter >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datacenter: (global VCenter datacenter)

    # < Cluster >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_cluster: (global VCenter cluster)

    # < Datastore >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datastore: (global VCenter datastore)


    # < KVM cpuset information >
    #
    # cpuset: [ 4, 5, 6, 7, 8, 9 ]



    # < Failed login attempts >
    # Lockout the user after reaching the number of failed login attempts
    #
    #failed_login_attempts: 5

    # < Failed login lockout time >
    # Lockout time after failed login attempts
    #
    #failed_login_lockout_time: 6000

    # < Advanced API Access Logging >
    # Enables advance API access logging
    #
    #advanced_api_access_logging: true

    # < tls version >
    # TLS version
    #
    #tls_version: "1.2"
    # < Root certificate path >
    # Root certificate
    #
    #ca_certificate_path: ""

    # < intermediate certificate path >
    # intermediate certificate
    #
    #intermediate_certificate_path: ""

    # < certificate path >
    # certificate
    #
    #certificate_path: ""




    credentials_set: ComponentCredentials




#
# VSD 2
#
-
    # < Hostname >
    #
    hostname: "vsd2.example.met"

    # < VM name >
    # Used to identify VM on the hypervisor/vcenter.
    #
    vmname: "vsd2"

    # < Upgrade VM name >
    #
    upgrade_vmname: "new-vsd2"

    # < Server type >
    # (kvm, vcenter, heat)
    #
    target_server_type: "kvm"

    # < Server hostname >
    #
    target_server: "127.0.0.1"

    # < Bridge interface >
    #
    # mgmt_bridge: (global Bridge interface)

    # < Management IP address >
    #
    mgmt_ip: "192.168.1.112"

    # < Management IP prefix length >
    #
    mgmt_ip_prefix: 24

    # < Management IP gateway >
    #
    mgmt_gateway: "192.168.1.1"

    # < Datacenter >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datacenter: (global VCenter datacenter)

    # < Cluster >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_cluster: (global VCenter cluster)

    # < Datastore >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datastore: (global VCenter datastore)

#
# VSD 3
#
-
    # < Hostname >
    #
    hostname: "vsd3.example.met"

    # < VM name >
    # Used to identify VM on the hypervisor/vcenter.
    #
    vmname: "vsd3"

    # < Upgrade VM name >
    #
    upgrade_vmname: "new-vsd3"

    # < Server type >
    # (kvm, vcenter, heat)
    #
    target_server_type: "kvm"

    # < Server hostname >
    #
    target_server: "127.0.0.1"

    # < Bridge interface >
    #
    # mgmt_bridge: (global Bridge interface)

    # < Management IP address >
    #
    mgmt_ip: "192.168.1.113"

    # < Management IP prefix length >
    #
    mgmt_ip_prefix: 24

    # < Management IP gateway >
    #
    mgmt_gateway: "192.168.1.1"

    # < Datacenter >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datacenter: (global VCenter datacenter)

    # < Cluster >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_cluster: (global VCenter cluster)

    # < Datastore >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datastore: (global VCenter datastore)
