###############################################################################
# Virtualized Services Controller
#
# Specify configuration for Nuage VSCs.
#
# Automatically generated by conversion from /Users/mpiecuch/Downloads/build_vars_vnsaapoc.yml.
#



#
# VSC 1
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc1.mgmt.vnspoc.net"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "10.197.62.111"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "10.197.62.1"

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "10.164.62.111"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    #####################

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "1.1.1.2"

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    vmname: "vsc1.vnspoc"

    # < XMPP username >
    # Username used to identify the XMPP connection with VSD
    #
    xmpp_username: "vsc1"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm, vcenter, heat, aws) 
    #
    target_server_type: "kvm"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server
    #
    target_server: "10.197.62.2"

    ###################

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    mgmt_bridge: "br0"

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    vsd_fqdn: "xmpp.mgmt.vnspoc.net"

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    # vcenter_datacenter: (global vCenter Datacenter Name)

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    # vcenter_cluster: (global vCenter Cluster Name)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    # vcenter_datastore: (global vCenter Datastore Name)

    ########################

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### AWS Parameters

    # < Private Management IP >
    # Private Management IP Address of VSC Instances on AWS
    #
    # internal_mgmt_ip: ""

    # < Management Routed Network Name >
    # Name of routed network for VSC Management Interface on AWS
    #
    # mgmt_routed_network_name: ""

    # < Data Routed Network Nmae >
    # Name of routed network for AWS VSC Data Interface on AWS
    #
    # data_routed_network_name: ""

    # < Private Control IP Address >
    # Control IP Address of VSC Instances on AWS
    #
    # internal_ctrl_ip: ""

    ####################


#
# VSC 2
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc2.mgmt.vnspoc.net"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "10.197.62.112"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "10.197.62.1"

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "10.164.62.112"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    #####################

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "1.1.1.3"

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    vmname: "vsc2.vnspoc"

    # < XMPP username >
    # Username used to identify the XMPP connection with VSD
    #
    xmpp_username: "vsc2"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm, vcenter, heat, aws) 
    #
    target_server_type: "kvm"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server
    #
    target_server: "10.197.62.2"

    ###################

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    mgmt_bridge: "br0"

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    vsd_fqdn: "xmpp.mgmt.vnspoc.net"

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    # vcenter_datacenter: (global vCenter Datacenter Name)

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    # vcenter_cluster: (global vCenter Cluster Name)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    # vcenter_datastore: (global vCenter Datastore Name)

    ########################

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### AWS Parameters

    # < Private Management IP >
    # Private Management IP Address of VSC Instances on AWS
    #
    # internal_mgmt_ip: ""

    # < Management Routed Network Name >
    # Name of routed network for VSC Management Interface on AWS
    #
    # mgmt_routed_network_name: ""

    # < Data Routed Network Nmae >
    # Name of routed network for AWS VSC Data Interface on AWS
    #
    # data_routed_network_name: ""

    # < Private Control IP Address >
    # Control IP Address of VSC Instances on AWS
    #
    # internal_ctrl_ip: ""

    ####################


#
# VSC 3
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc5.mgmt.vnspoc.net"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "10.197.62.115"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "10.197.62.1"

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "10.164.62.115"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    #####################

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "1.1.1.6"

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    vmname: "vsc5.vnspoc"

    # < XMPP username >
    # Username used to identify the XMPP connection with VSD
    #
    xmpp_username: "vsc5"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm, vcenter, heat, aws) 
    #
    target_server_type: "kvm"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server
    #
    target_server: "10.197.62.2"

    ###################

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    mgmt_bridge: "br0"

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    vsd_fqdn: "xmpp.mgmt.vnspoc.net"

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    # vcenter_datacenter: (global vCenter Datacenter Name)

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    # vcenter_cluster: (global vCenter Cluster Name)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    # vcenter_datastore: (global vCenter Datastore Name)

    ########################

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### AWS Parameters

    # < Private Management IP >
    # Private Management IP Address of VSC Instances on AWS
    #
    # internal_mgmt_ip: ""

    # < Management Routed Network Name >
    # Name of routed network for VSC Management Interface on AWS
    #
    # mgmt_routed_network_name: ""

    # < Data Routed Network Nmae >
    # Name of routed network for AWS VSC Data Interface on AWS
    #
    # data_routed_network_name: ""

    # < Private Control IP Address >
    # Control IP Address of VSC Instances on AWS
    #
    # internal_ctrl_ip: ""

    ####################


#
# VSC 4
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc6.mgmt.vnspoc.net"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "10.197.62.116"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "10.197.62.1"

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "10.164.62.116"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    #####################

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "1.1.1.7"

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    vmname: "vsc6.vnspoc"

    # < XMPP username >
    # Username used to identify the XMPP connection with VSD
    #
    xmpp_username: "vsc6"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm, vcenter, heat, aws) 
    #
    target_server_type: "kvm"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server
    #
    target_server: "10.197.62.2"

    ###################

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    mgmt_bridge: "br0"

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    vsd_fqdn: "xmpp.mgmt.vnspoc.net"

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    # vcenter_datacenter: (global vCenter Datacenter Name)

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    # vcenter_cluster: (global vCenter Cluster Name)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    # vcenter_datastore: (global vCenter Datastore Name)

    ########################

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### AWS Parameters

    # < Private Management IP >
    # Private Management IP Address of VSC Instances on AWS
    #
    # internal_mgmt_ip: ""

    # < Management Routed Network Name >
    # Name of routed network for VSC Management Interface on AWS
    #
    # mgmt_routed_network_name: ""

    # < Data Routed Network Nmae >
    # Name of routed network for AWS VSC Data Interface on AWS
    #
    # data_routed_network_name: ""

    # < Private Control IP Address >
    # Control IP Address of VSC Instances on AWS
    #
    # internal_ctrl_ip: ""

    ####################


#
# VSC 5
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc3.mgmt.vnspoc.net"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "10.197.62.113"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "10.197.62.1"

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "10.164.62.113"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    #####################

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "1.1.1.4"

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    vmname: "vsc3.vnspoc"

    # < XMPP username >
    # Username used to identify the XMPP connection with VSD
    #
    xmpp_username: "vsc3"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm, vcenter, heat, aws) 
    #
    target_server_type: "kvm"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server
    #
    target_server: "10.197.62.2"

    ###################

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    mgmt_bridge: "br0"

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    vsd_fqdn: "xmpp.mgmt.vnspoc.net"

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    # vcenter_datacenter: (global vCenter Datacenter Name)

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    # vcenter_cluster: (global vCenter Cluster Name)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    # vcenter_datastore: (global vCenter Datastore Name)

    ########################

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### AWS Parameters

    # < Private Management IP >
    # Private Management IP Address of VSC Instances on AWS
    #
    # internal_mgmt_ip: ""

    # < Management Routed Network Name >
    # Name of routed network for VSC Management Interface on AWS
    #
    # mgmt_routed_network_name: ""

    # < Data Routed Network Nmae >
    # Name of routed network for AWS VSC Data Interface on AWS
    #
    # data_routed_network_name: ""

    # < Private Control IP Address >
    # Control IP Address of VSC Instances on AWS
    #
    # internal_ctrl_ip: ""

    ####################


#
# VSC 6
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc4.mgmt.vnspoc.net"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "10.197.62.114"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "10.197.62.1"

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "10.164.62.114"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    #####################

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "1.1.1.5"

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    vmname: "vsc4.vnspoc"

    # < XMPP username >
    # Username used to identify the XMPP connection with VSD
    #
    xmpp_username: "vsc4"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm, vcenter, heat, aws) 
    #
    target_server_type: "kvm"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server
    #
    target_server: "10.197.62.2"

    ###################

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    mgmt_bridge: "br0"

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    vsd_fqdn: "xmpp.mgmt.vnspoc.net"

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    # vcenter_datacenter: (global vCenter Datacenter Name)

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    # vcenter_cluster: (global vCenter Cluster Name)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    # vcenter_datastore: (global vCenter Datastore Name)

    ########################

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### AWS Parameters

    # < Private Management IP >
    # Private Management IP Address of VSC Instances on AWS
    #
    # internal_mgmt_ip: ""

    # < Management Routed Network Name >
    # Name of routed network for VSC Management Interface on AWS
    #
    # mgmt_routed_network_name: ""

    # < Data Routed Network Nmae >
    # Name of routed network for AWS VSC Data Interface on AWS
    #
    # data_routed_network_name: ""

    # < Private Control IP Address >
    # Control IP Address of VSC Instances on AWS
    #
    # internal_ctrl_ip: ""

    ####################


#
# VSC 7
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc7.mgmt.vnspoc.net"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "10.197.62.117"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "10.197.62.1"

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "10.164.62.117"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    #####################

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "1.1.1.8"

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    vmname: "vsc7.vnspoc"

    # < XMPP username >
    # Username used to identify the XMPP connection with VSD
    #
    xmpp_username: "vsc7"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm, vcenter, heat, aws) 
    #
    target_server_type: "kvm"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server
    #
    target_server: "10.197.62.2"

    ###################

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    mgmt_bridge: "br0"

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    vsd_fqdn: "xmpp.mgmt.vnspoc.net"

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    # vcenter_datacenter: (global vCenter Datacenter Name)

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    # vcenter_cluster: (global vCenter Cluster Name)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    # vcenter_datastore: (global vCenter Datastore Name)

    ########################

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### AWS Parameters

    # < Private Management IP >
    # Private Management IP Address of VSC Instances on AWS
    #
    # internal_mgmt_ip: ""

    # < Management Routed Network Name >
    # Name of routed network for VSC Management Interface on AWS
    #
    # mgmt_routed_network_name: ""

    # < Data Routed Network Nmae >
    # Name of routed network for AWS VSC Data Interface on AWS
    #
    # data_routed_network_name: ""

    # < Private Control IP Address >
    # Control IP Address of VSC Instances on AWS
    #
    # internal_ctrl_ip: ""

    ####################


#
# VSC 8
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc8.mgmt.vnspoc.net"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "10.197.62.118"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "10.197.62.1"

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "10.164.62.118"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    #####################

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "1.1.1.9"

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    vmname: "vsc8.vnspoc"

    # < XMPP username >
    # Username used to identify the XMPP connection with VSD
    #
    xmpp_username: "vsc8"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm, vcenter, heat, aws) 
    #
    target_server_type: "kvm"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server
    #
    target_server: "10.197.62.2"

    ###################

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    mgmt_bridge: "br0"

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    vsd_fqdn: "xmpp.mgmt.vnspoc.net"

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    # vcenter_datacenter: (global vCenter Datacenter Name)

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    # vcenter_cluster: (global vCenter Cluster Name)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    # vcenter_datastore: (global vCenter Datastore Name)

    ########################

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### AWS Parameters

    # < Private Management IP >
    # Private Management IP Address of VSC Instances on AWS
    #
    # internal_mgmt_ip: ""

    # < Management Routed Network Name >
    # Name of routed network for VSC Management Interface on AWS
    #
    # mgmt_routed_network_name: ""

    # < Data Routed Network Nmae >
    # Name of routed network for AWS VSC Data Interface on AWS
    #
    # data_routed_network_name: ""

    # < Private Control IP Address >
    # Control IP Address of VSC Instances on AWS
    #
    # internal_ctrl_ip: ""

    ####################


