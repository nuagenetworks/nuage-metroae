###############################################################################
# VSCs
#
# Nuage MetroAG VSC settings
#
# Automatically generated by conversion from /Users/mpiecuch/nuage-metro-tests/robot/resources/templates/kvm_sa/build_vars_kvm.yml.
#

#
# VSC 1
#
-
    # < Hostname >
    #
    hostname: "jen-tb1-c7-b-vsc.mag.net"

    # < VM name >
    # Used to identify VM on the hypervisor/vcenter.
    #
    vmname: "jenkinsvsc1"

    # < Server type >
    # (kvm, vcenter, heat)
    #
    target_server_type: "kvm"

    # < Server hostname >
    #
    target_server: "10.106.12.100"

    # < Bridge interface >
    #
    # mgmt_bridge: (global Bridge interface)

    # < Management IP address >
    #
    mgmt_ip: "10.106.12.201"

    # < Management IP prefix length >
    #
    mgmt_ip_prefix: 24

    # < Management IP gateway >
    #
    mgmt_gateway: "10.106.12.100"

    # < VSD fqdn >
    #
    vsd_fqdn: "jen-tb1-c7-a-vsd.mag.net"

    # < Routing IP address >
    # Only required when IGP or BGP routing will be configured
    #
    system_ip: "1.1.1.2"

    # < XMPP username >
    #
    xmpp_username: "jenkinsvsc1"

    # < Static route IP prefixes >
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.1/1 ]

    # < Control IP address >
    #
    ctrl_ip: "10.106.100.202"

    # < Control netmask prefix length >
    #
    ctrl_ip_prefix: 24

    # < VCenter Username >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_username: (global VCenter username)

    # < Password >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_password: (global VCenter password)

    # < Datacenter >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datacenter: (global VCenter datacenter)

    # < Cluster >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_cluster: (global VCenter cluster)

    # < Datastore >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datastore: (global VCenter datastore)

    # < Expected number BGP peers >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0


#
# VSC 2
#
-
    # < Hostname >
    #
    hostname: "jen-tb1-c7-c-vsc.mag.net"

    # < VM name >
    # Used to identify VM on the hypervisor/vcenter.
    #
    vmname: "jenkinsvsc2"

    # < Server type >
    # (kvm, vcenter, heat)
    #
    target_server_type: "kvm"

    # < Server hostname >
    #
    target_server: "10.106.13.100"

    # < Bridge interface >
    #
    # mgmt_bridge: (global Bridge interface)

    # < Management IP address >
    #
    mgmt_ip: "10.106.13.201"

    # < Management IP prefix length >
    #
    mgmt_ip_prefix: 24

    # < Management IP gateway >
    #
    mgmt_gateway: "10.106.13.100"

    # < VSD fqdn >
    #
    vsd_fqdn: "jen-tb1-c7-a-vsd.mag.net"

    # < Routing IP address >
    # Only required when IGP or BGP routing will be configured
    #
    system_ip: "1.1.1.3"

    # < XMPP username >
    #
    xmpp_username: "jenkinsvsc2"

    # < Static route IP prefixes >
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.1/1 ]

    # < Control IP address >
    #
    ctrl_ip: "10.106.100.203"

    # < Control netmask prefix length >
    #
    ctrl_ip_prefix: 24

    # < VCenter Username >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_username: (global VCenter username)

    # < Password >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_password: (global VCenter password)

    # < Datacenter >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datacenter: (global VCenter datacenter)

    # < Cluster >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_cluster: (global VCenter cluster)

    # < Datastore >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datastore: (global VCenter datastore)

    # < Expected number BGP peers >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0


