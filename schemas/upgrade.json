{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "$id": "urn:nuage-metroae:upgrade",
    "title": "Upgrade",
    "description": "Nuage MetroAE upgrade settings",
    "type": "object",
    "widget": "form",
    "definitions": {
        "version": {
            "type": "string",
            "pattern": "[0-9]+[.][0-9]+[.][0-9a-zA-Z]+"
        }
    },
    "properties": {
        "upgrade_from_version": {
            "$ref": "#/definitions/version",
            "title": "Upgrade starting version",
            "description": "Version VSD is being upgraded from. Required for VSD stats (VSTAT) upgrade.",
            "propertyOrder": 10
        },
        "upgrade_to_version": {
            "$ref": "#/definitions/version",
            "title": "Upgrade desired version",
            "description": "Version VSD is being upgraded to. Required for VSD stats (VSTAT) upgrade.",
            "propertyOrder": 20
        },
        "vstat_nfs_server": {
            "type": "string",
            "default": "0.0.0.0",
            "format": "hostname",
            "title": "VSTAT NFS server",
            "description": "Used to mount NFS folder on VSD stats (VSTAT) node(s).",
            "propertyOrder": 30
        },
        "vstat_nfs_folder": {
            "type": "string",
            "default": "/tmp/vstat/",
            "title": "VSTAT NFS directory",
            "description": "Used to mount NFS directory on VSD stats (VSTAT) node(s).",
            "propertyOrder": 40
        },
        "metro_backup_root": {
            "type": "string",
            "default": "(unzip dir)/backups",
            "title": "Backup directory root",
            "description": "The location for backups during upgrade.",
            "propertyOrder": 50
        }
    },
    "required": ["upgrade_from_version", "upgrade_to_version"]
}
