# Commands file
#
# This file contains commands that are to be run post-deployment

# The commands to be run for VSC verification
vsc_commands:
  - "show vswitch-controller xmpp"
  - "show router bgp summary all"

# The commands to be run for VRS verification
vrs_commands:
  - "show vswitch-controller vswitches"
