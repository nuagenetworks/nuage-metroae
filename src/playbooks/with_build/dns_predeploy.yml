- hosts: dnss

  gather_facts: no

  pre_tasks:

    - name: Check if DNS prereq satisfied for predeploy

      include_role:

        name: common

        tasks_from: check-predeploy-prereq

  roles:

    - dns-predeploy
