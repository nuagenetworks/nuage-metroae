- hosts: vsds
  gather_facts: no
  include_role:
    name: common
    tasks_from: sysbench-test.yml
  vars:
    file_total_size: "{{ sysbench_file_total_size }}"
    iops_target_threshold: "{{ sysbench_iops_target_threshold }}"
