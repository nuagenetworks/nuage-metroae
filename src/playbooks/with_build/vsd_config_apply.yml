---
- hosts: localhost
  gather_facts: no
  pre_tasks:
    
    - name: Check if the user data is defined
      meta: end_play
      when: not (config_user_data_path is defined)

  roles:
    - vsd-config-apply
