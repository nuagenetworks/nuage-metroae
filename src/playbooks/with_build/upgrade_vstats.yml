---
- hosts: vstats
  gather_facts: no
  tasks:
    - name: Check if VSTATs prereq satisfied for predeploy
      include_role:
        name: common
        tasks_from: check-predeploy-prereq

- include: "vstat_upgrade_backup_and_prep.yml"
- include: "vstat_upgrade.yml"
- include: "vstat_upgrade_wrapup.yml"
