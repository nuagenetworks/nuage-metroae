---
- hosts: vsd_ha_node1
  gather_facts: no
  pre_tasks:
    - block:
      - name: Set interfaces Up for the predeployed vsd
        include_role:
          name: vsd-predeploy
          tasks_from: vcenter-enable-interface

      - name: Skip the play since predeploy is already executed
        meta: end_host

      when: vcenter_start_with_interfaces_disabled | default (False)    - name: Set upgrade flag
      set_fact:
        nuage_upgrade: true

    - name: Check if prereq satisfied for upgrade
      include_role:
        name: common
        tasks_from: check-upgrade-prereq

    - name: Check if VSD prereq satisfied for predeploy
      include_role:
        name: common
        tasks_from: check-predeploy-prereq

  roles:
    - vsd-predeploy
