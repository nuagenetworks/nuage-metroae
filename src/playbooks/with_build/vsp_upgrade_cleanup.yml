---

- hosts: localhost
  tasks:
  - include_role:
      name: vsd-upgrade-cleanup
  - include_role:
      name: vsc-upgrade-cleanup
  vars:
    delete_everything: "{{ delete_all_backups | default('false') }}"
