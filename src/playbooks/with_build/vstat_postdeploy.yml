---
- hosts: vstats
  roles:
    - vstat-postdeploy
  become: yes
  remote_user: "{{ vstat_default_username }}"
  gather_facts: no

- hosts: primary_vstats
  roles:
    - vstat-postdeploy
  become: yes
  remote_user: "{{ vstat_default_username }}"
  gather_facts: no

- hosts: backup_vstats
  roles:
    - vstat-postdeploy
  become: yes
  remote_user: "{{ vstat_default_username }}"
  gather_facts: no
