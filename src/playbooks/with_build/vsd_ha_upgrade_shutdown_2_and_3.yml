---
- hosts: vsd_ha_node2:vsd_ha_node3
  serial: 1
  gather_facts: no
  pre_tasks:
    - name: Set vm name, preserve vm and upgrade flag
      set_fact:
        nuage_upgrade: true
        vm_name: "{{ vmname }}"
        preserve_vm: true

  roles:
    - vsd-services-stop
    - vsd-destroy
