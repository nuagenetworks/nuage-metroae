---

- hosts: vnsutils
  gather_facts: no
  pre_tasks:
    - name: Check if VNS Util prereq satisfied for install
      include_role:
        name: common
        tasks_from: check-predeploy-prereq

- hosts: nsgvs
  gather_facts: no
  pre_tasks:
    - name: Check if NSGVs prereq satisfied for install
      include_role:
        name: common
        tasks_from: check-predeploy-prereq

- include: install_dns.yml

- include: install_vcs.yml

- include: install_vns.yml

- include: install_osc.yml
