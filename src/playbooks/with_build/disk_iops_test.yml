- hosts: vsds
  gather_facts: no
  include_role:
    name: common
    tasks_from: disk-iops-test.yml
  vars:
    file_total_size: "{{ disk_iops_file_total_size }}"
    iops_target_threshold: "{{ disk_iops_target_threshold }}"
