
- hosts: primary_vsds
  gather_facts: no
  roles:
    - vsd-upgrade-prepare-for-deploy
  vars:
    restore_dir: "{{ backup_to_restore }}/vsd-*"

- name: Run VSD Deploy
  import_playbook: "vsd_deploy.yml"
