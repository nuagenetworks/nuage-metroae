---
- hosts: vsds
  gather_facts: no
  tasks:
   - include_role:
        name: netconf-manager-deploy
        tasks_from: create_netconf_user

- hosts: nuhs
  gather_facts: no
  roles:
    - netconf-manager-deploy
