- include_role:
    name: check-skip-target-server
  vars:
    - target_server_list: ["kvm", "vcenter"]
    - target_server_role: "{{ ansible_play_role_names }}"

- include_tasks: "{{ target_server_type }}.yml"
