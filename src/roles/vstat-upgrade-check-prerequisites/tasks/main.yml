- include: vstat_in_place_upgrade.yml
  remote_user: root
  when: vstat_in_place_upgrade

- name: Backup elasticsearch data
  include_role:
    name: vstat-data-backup
  vars:
    nuage_upgrade: true
  when: not vstat_in_place_upgrade
