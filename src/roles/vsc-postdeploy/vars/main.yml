---
# Post Deploy specific Variables
vsc_creds:
  host: "{{ mgmt_ip }}"
  username: "{{ vsc_default_username }}"
  password: "{{ vsc_default_password }}"
  timeout: 180
# Timeout in seconds
retry_timeout: 300
