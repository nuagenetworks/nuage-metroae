- name: Run if target_server_type is in the list
  block:

  - name: Display if skipping role
    debug:
      msg:
        - "****************************************************************************"
        - "Skipping {{ target_server_role|default('role') because {{ target_server_type }} is not supported"
        - "****************************************************************************"

  - name: Stop role
    meta: end_play

  when:
    - target_server_list is defined
    - target_server_type in target_server_list
