;
$TTL 3D
@        IN       SOA    {{ dns_mgmt.split('.')[1:]|join('.') }} {{ dns_mgmt.split('.')[1:]|join('.') }} (
199802151     ; Serial
8H     ; Refresh
2H     ; Retry
4W     ; Expire
1D)     ; Minimum TTL
;

        NS      {{ dns_mgmt.split('.')[1:]|join('.') }}

{% for key, value in dns_mgmt_lookup.items() %}
{{ value.split('.')[3] }}     IN     PTR     {{ key }}
{% endfor %}
