- block:

  - name: Check is ejabberd license is expired
    shell: /opt/ejabberd/bin/ejabberdctl license_info
    register: vsd_license_check

  - block:

    when: vsd_ejabberd_license_file is defined

  remote_user: "{{ vsd_default_username }"