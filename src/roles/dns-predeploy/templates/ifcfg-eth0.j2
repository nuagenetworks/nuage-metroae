DEVICE="eth0"
IPV6INIT="yes"
NM_CONTROLLED="no"
ONBOOT="yes"
TYPE="Ethernet"
BOOTPROTO="static"
IPADDR="{{ mgmt_ip }}"
GATEWAY="{{ mgmt_gateway }}"
PREFIX="{{ mgmt_prefix }}"
DNS1="{{ dns_server }}"
DNS2="{{ mgmt_ip }}"

