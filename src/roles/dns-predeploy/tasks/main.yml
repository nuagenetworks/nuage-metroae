- include_role:
    name: check-skip-target-server
  vars:
    - target_server_list: ["kvm"]
    - target_server_role: "{{ ansible_play_name }}"

- import_tasks: kvm.yml
  tags:
    - dns
    - dns-predeploy
