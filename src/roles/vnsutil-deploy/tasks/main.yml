- name: Clean known_hosts of VNSUTILs (ignoring errors)
  known_hosts:
    name: "{{ mgmt_ip }}"
    state: absent
  delegate_to: localhost
  no_log: True
  ignore_errors: True

- import_tasks: non_heat.yml
  when: not target_server_type is match("heat")
  tags:
    - vnsutil
    - vnsutil-deploy
