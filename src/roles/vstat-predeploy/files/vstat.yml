heat_template_version: '2014-10-16'

parameters:

  vm_name:

    type: string

  ssh_key:

    type: string

  vstat_image:

    type: string

  vstat_network:

    type: string

  vstat_subnet:

    type: string

  vstat_flavor:

    type: string

  mgmt_ip:

    type: string

 

resources:

  mycompute:

    type: OS::Nova::Server

    properties:

      name: {get_param: vm_name}

      flavor: {get_param: vstat_flavor}

      image: {get_param: vstat_image}

      networks:

      - network: {get_param: vstat_network}

      user_data_format: RAW

      user_data: 

        str_replace:

          template: |

            #!/bin/bash

            mkdir /root/.ssh/

            echo usr >> /root/.ssh/authorized_keys

          params:

            usr: {get_param: ssh_key} 

outputs:

  server_ip:

    description: mgmt ip assigned to vstat

    value: { get_attr: [mycompute, networks, {get_param: vstat_network}, 0]}
