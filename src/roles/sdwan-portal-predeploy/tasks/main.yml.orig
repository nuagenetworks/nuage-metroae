---
<<<<<<< HEAD
- include_role:
    name: check-node-reachability

- import_tasks: kvm.yml
  when: target_server_type | match("kvm")
=======

- include_role:
    name: check-node-reachability

- name: Set fact for PORTAL present
  set_fact:
    node_present: "{{ portal_file is defined and
                      portal_file.stat is defined and
                      portal_file.stat.exists is defined and
                      portal_file.stat.exists }}"

- import_tasks: kvm.yml
  when: target_server_type | match("kvm")

>>>>>>> HA Portal install and VM deployment
