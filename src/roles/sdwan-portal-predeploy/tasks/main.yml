- include_role:
    name: check-skip-target-server
  vars:
    - target_server_list: ["kvm"]
    - target_server_role: "{{ ansible_play_role_names }}"
  
- include_role:
    name: check-node-reachability

- import_tasks: kvm.yml
