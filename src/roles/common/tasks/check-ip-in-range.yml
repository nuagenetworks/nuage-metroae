---
- name: check ip address in range
  assert: 
    that: {{ (ip_address | ( netaddr ~ '/' ~ netmask)) != None }}
    msg: "IP Address {{ ip_address }} not in network range defined by network address {{ netaddr }} and mask {{ netmask }}"
