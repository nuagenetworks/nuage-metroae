- name: Get the VSD version
  shell: |
    set -o pipefail
    grep -ir "VSD_VERSION=" /etc/profile.d/vsd-banner.sh | awk -F "=" '{print $2}'
  changed_when: False
  register: vsd_version
  run_once: true
