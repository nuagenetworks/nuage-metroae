- block:
  - name: Create host_vars files for vns portal
    template: src=portal.j2 backup=no dest={{ inventory_dir }}/host_vars/{{ item.hostname }}
    with_items: "{{ portals }}"
  when: portals is defined and portals

