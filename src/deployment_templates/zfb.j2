###############################################################################
# Upgrade
#
# Nuage MetroAE zero-factor bootstrap settings
#
# Automatically generated by {{ generator_script | default("script") }}.
#

# < VSD License File >
# Path to the VSD license file on the MetroAE host
#
vsd_license_file: "{{ vsd_license_file }}"

# < NSG Organization >
# Organization on the VSD to associate with the NSG being bootstraped
#
nsg_organization: "{{ nsg_organization }}"

# < NSG Name >
# Name of the NSG on the VSD
#
nsg_name: "{{ nsg_name }}"

# < Proxy User's First Name >
# First name of the user to bootstrap the NSG
#
nsg_proxy_user_first_name: "{{ nsg_proxy_user_first_name }}"

# < Proxy User's Last Name >
# Last name of the user to bootstrap the NSG
#
nsg_proxy_user_last_name: "{{ nsg_proxy_user_last_name }}"

# < Proxy User's Email Address >
# Email address of the user to bootstrap the NSG
#
nsg_proxy_user_email: "{{ nsg_proxy_user_email }}"

# < Proxy User's Password >
# Password of the user to bootstrap the NSG
#
nsg_proxy_user_password: "{{ nsg_proxy_user_password }}"

# < NSG Infra Profile Name >
# Name of the NSG infra profile
#
nsg_infra_profile_name: "{{ nsg_infra_profile_name }}"

# < NSG Infra Profile Template Name >
# Name of the NSG infra profile template
#
nsg_infra_profile_nsg_template_name: "{{ nsg_infra_profile_nsg_template_name }}"

# < NSG Infra Profile Proxy DNS Name >
# Name of the NSG infra profile proxy DNS
#
nsg_infra_profile_proxy_dns_name: "{{ nsg_infra_profile_proxy_dns_name }}"

# < VSC Infra Profile Name >
# Name of the VSC infra profile for the NSG
#
vsc_infra_profile_name: "{{ vsc_infra_profile_name }}"

# < VSC Infra Profile First Controller >
# Host name or IP address of the VSC infra profile first controller for the NSG
#
vsc_infra_profile_first_controller: "{{ vsc_infra_profile_first_controller }}"

# < VSC Infra Profile Second Controller >
# Host name or IP address of the VSC infra profile second controller for the NSG
#
vsc_infra_profile_second_controller: "{{ vsc_infra_profile_second_controller }}"

# < NSG Network Port Name >
# Name of the network port for the NSG
#
nsg_network_port_name: "{{ nsg_network_port_name }}"

# < NSG Network Port Physical Name >
# Physical name of the network port for the NSG
#
nsg_network_port_physical_name: "{{ nsg_network_port_physical_name }}"

# < NSG Access Port Name >
# Name of the access port for the NSG
#
nsg_access_port_name: "{{ nsg_access_port_name }}"

# < NSG Access Port Physical Name >
# Physical name of the access port for the NSG
#
nsg_access_port_physical_name: "{{ nsg_access_port_physical_name }}"

# < NSG Access Port VLAN Range >
# VLAN range of the access port for the NSG
#
nsg_access_port_vlan_range: "{{ nsg_access_port_vlan_range }}"

# < NSG Access Port VLAN Number >
# VLAN number of the NSG access port for the NSG
#
nsg_access_port_vlan_number: "{{ nsg_access_port_vlan_number }}"

