###############################################################################
# Nuage SD-WAN Portal GRCN
#
# Configuration for Nuage SD-WAN Portal GRCN
#
# Automatically generated by {{ generator_script | default("script") }}.
#

{% if portal_grcns is defined and portal_grcns %}
{% for item in portal_grcns %}
#
# Portal GRCN {{ loop.index }}
#
-
    ##### Management Interface

    # < SD-WAN Portal GRCN IP or FQDN >
    # SD-WAN Portal GRCN VM hostname or IP address
    #
    hostname: "{{ item.hostname }}"

    # < Management IP Address >
    # Management IP of SD-WAN Portal GRCN
    #
    {%- if item.mgmt_ip is defined %}
    mgmt_ip: "{{ item.mgmt_ip }}"
    {%- else %}
    # mgmt_ip: ""
    {%- endif %}

    # < Management Network Prefix Length >
    # Management network prefix length for SD-WAN Portal GRCN
    #
    {%- if item.mgmt_ip_prefix is defined %}
    mgmt_ip_prefix: {{ item.mgmt_ip_prefix }}
    {%- else %}
    # mgmt_ip_prefix: 0
    {%- endif %}

    # < Management Network Gateway >
    # Management network gateway for SD-WAN Portal GRCN
    #
    {%- if item.mgmt_gateway is defined %}
    mgmt_gateway: "{{ item.mgmt_gateway }}"
    {%- else %}
    # mgmt_gateway: ""
    {%- endif %}

    # < Management Network Bridge >
    # Network Bridge used for the management interface of Portal GRCN. It is a Linux network bridge. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    {%- if item.mgmt_bridge is defined %}
    mgmt_bridge: "{{ item.mgmt_bridge }}"
    {%- else %}
    # mgmt_bridge: (global Bridge interface)
    {%- endif %}

    ##########################

    ##### VM and Target Server

    # < VM name >
    # Name of the Virtual Machine on the Hypervisor
    #
    {%- if item.vmname is defined %}
    vmname: "{{ item.vmname }}"
    {%- else %}
    # vmname: (Hostname)
    {%- endif %}

    # < Target Server Type >
    # Type of hypervisor environment into which the instance will be created
    # (kvm) 
    #
    {%- if item.target_server_type is defined %}
    target_server_type: "{{ item.target_server_type }}"
    {%- else %}
    # target_server_type: ""
    {%- endif %}

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated
    #
    {%- if item.target_server is defined %}
    target_server: "{{ item.target_server }}"
    {%- else %}
    # target_server: ""
    {%- endif %}

    # < KVM cpuset information >
    # Cpuset information for cpu pinning on KVM. For example, Portal requires 6 cores and sample values will be of the form [ 0, 1, 2, 3, 4, 5 ]
    #
    {%- if item.cpuset is defined %}
    cpuset: [ {% for i in item.cpuset | default([]) %}{{ i }}, {% endfor %}]
    {%- else %}
    # cpuset: []
    {%- endif %}

    ##########################

    # < Current DC >
    # Name of the current datacenter
    #
    {%- if item.current_dc is defined %}
    current_dc: "{{ item.current_dc }}"
    {%- else %}
    # current_dc: ""
    {%- endif %}

    # < Other DC Portal1 IP >
    # Management IP of other datacenter's portal 1
    #
    {%- if item.other_dc_portal1_ip is defined %}
    other_dc_portal1_ip: "{{ item.other_dc_portal1_ip }}"
    {%- else %}
    # other_dc_portal1_ip: ""
    {%- endif %}

    # < Other DC Portal2 IP >
    # Management IP of other datacenter's portal 2
    #
    {%- if item.other_dc_portal2_ip is defined %}
    other_dc_portal2_ip: "{{ item.other_dc_portal2_ip }}"
    {%- else %}
    # other_dc_portal2_ip: ""
    {%- endif %}

    # < Other DC Portal3 IP >
    # Management IP of other datacenter's portal 3
    #
    {%- if item.other_dc_portal3_ip is defined %}
    other_dc_portal3_ip: "{{ item.other_dc_portal3_ip }}"
    {%- else %}
    # other_dc_portal3_ip: ""
    {%- endif %}

    # < Other DC GRCN IP >
    # Management IP of other datacenter's GRCN
    #
    {%- if item.other_dc_grcn_ip is defined %}
    other_dc_grcn_ip: "{{ item.other_dc_grcn_ip }}"
    {%- else %}
    # other_dc_grcn_ip: ""
    {%- endif %}

{% endfor %}
{% else %}
[ ]
{% endif %}
