---
- include: non_heat.yml
  when: not target_server_type | match("heat")
  tags:
    - vsd
    - vsd-deploy

- include: heat.yml
  when: target_server_type | match("heat")
  tags:
    - vsd
    - heat
    - vsd-deploy
