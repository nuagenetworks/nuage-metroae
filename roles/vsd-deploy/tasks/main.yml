---
- include: kvm.yml
  when: ("{{ target_server_type }}" == "kvm")
  tags:
    - vsd
    - vsd-deploy

- include: vcenter.yml
  when: ("{{ target_server_type }}" == "vcenter")
  tags:
    - vsd
    - vsd-deploy

- include: heat.yml
  when: ("{{ target_server_type }}" == "heat")
  tags:
    - vsd
    - heat
    - vsd-deploy
