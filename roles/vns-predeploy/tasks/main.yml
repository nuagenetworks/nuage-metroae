---
- include: kvm.yml
  when: ("{{ target_server_type }}" == "kvm")
  tags:
    - vns
    - vns-predeploy

- include: vcenter.yml
  when: ("{{ target_server_type }}" == "vcenter")
  tags:
    - vns
    - vns-predeploy
