- include: post_upgrade_checks.yml
  remote_user: root
  when: vstat_in_place_upgrade

- name: Migrate elasticsearch data to new vstat vm(s)
  include_role:
    name: vstat-data-migrate
  when: not vstat_in_place_upgrade

