---
- name: Stat the file
  stat:
    path: "{{ inventory_dir }}/{{ file_name }}"
    get_md5: yes
  register: data_file

- name: Write md5 file to disk
  copy: 
    content: "{{ data_file.stat.md5 }}"
    dest: "{{ inventory_dir }}/{{ file_name }}.md5"

