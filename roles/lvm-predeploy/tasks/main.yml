---
- include: kvm.yml
  when: target_server_type | match("kvm")
  tags:
    - lvm
    - lvm-predeploy

- include: vcenter.yml
  when: target_server_type | match("vcenter")
  tags:
    - lvm
    - lvm-predeploy
