#temp_dir: /tmp/vrs_packages
plugin_dir: /tmp/plugins
neutron_services:
  - neutron-dhcp-agent.service
  - neutron-l3-agent.service
  - neutron-metadata-agent.service
  - neutron-openvswitch-agent.service
  - neutron-netns-cleanup.service
  - neutron-ovs-cleanup.service
csp:
  username:  csproot
  password:  csproot
  enterprise: csp
vsd_mgmt_ip: "{{ vsd_mgmt_ip }}"
upgrade_cmd: "set_and_audit_cms.py --neutron-config-file /etc/neutron/neutron.conf --plugin-config-file /etc/neutron/plugin.ini"
nuage_target_architecture: "el7"
