---
- include: kvm.yml
  when: ("{{ target_server_type }}" == "kvm")
  tags:
    - osc
    - osc-predeploy

- include: heat.yml
  when: ("{{ target_server_type }}" == "heat")
  tags:
    - osc
    - heat
    - osc-predeploy
