---
- include: kvm.yml
  when: target_server_type | match("kvm")
  tags:
    - gvm
    - gvm-predeploy

- include: vcenter.yml
  when: target_server_type | match("vcenter")
  tags:
    - gvm
    - gvm-predeploy
