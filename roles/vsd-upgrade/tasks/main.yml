---
# TODO: Add Vmware support
- include: kvm.yml
  when: ("{{ target_server_type }}" == "kvm")
  tags:
    - vsd
    - vsd-upgrade

- include: heat.yml
  when: ("{{ target_server_type }}" == "heat")
  tags:
    - vsd
    - heat
    - vsd-upgrade
