iptable_entries:
  - iptables -I INPUT -s "{{ hostvars[vsd_node2]['mgmt_ip'] }}" -j DROP
  - iptables -I OUTPUT -d "{{ hostvars[vsd_node2]['mgmt_ip'] }}" -j DROP
  - iptables -I INPUT -s "{{ hostvars[vsd_node3]['mgmt_ip'] }}" -j DROP
  - iptables -I OUTPUT -d "{{ hostvars[vsd_node3]['mgmt_ip'] }}" -j DROP
