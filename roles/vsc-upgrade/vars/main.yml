vsc_creds:
  host: "{{ mgmt_ip }}"
  username: "{{ vsc_user|default('admin') }}"
  password: "{{ vsc_password|default('admin') }}"
  transport: cli
  timeut: 180
vsc_image_copy: 'scp {{ vsc_tim_path }}/{{ vsc_tim_file_name }} admin@{{ mgmt_ip }}:"{{ bof_json.image_folder }}"'
vsc_scp_timeout: 720
expected_xmpp_server_state: "Functional"
