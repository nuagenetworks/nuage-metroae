---
- name: print version
  debug: var=ansible_version

- name: Check if the ansible version satisfies the minimum requirement
  fail: msg="Minimum required Ansible version is{{nuage_min_ansible_version}}"
  when: ansible_version.full < nuage_min_ansible_version
...
