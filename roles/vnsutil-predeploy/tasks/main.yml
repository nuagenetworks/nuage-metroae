---
- include: kvm.yml
  when: target_server_type == "kvm"
  tags:
    - vnsutil
    - vnsutil-predeploy

- include: vcenter.yml
  when: target_server_type == "vcenter"
  static: no
  tags:
    - nsgv
    - nsgv-predeploy
