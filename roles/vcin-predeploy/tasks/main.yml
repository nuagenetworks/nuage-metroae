---
- include: kvm.yml
  when: ("{{ target_server_type }}" == "kvm")
  tags:
    - vcin
    - vcin-predeploy

- include: heat.yml
  when: ("{{ target_server_type }}" == "heat")
  tags:
    - vcin
    - heat
    - vcin-predeploy

- include: vcenter.yml
  when: ("{{ target_server_type }}" == "vcenter")
  tags:
    - vcin
    - vcin-predeploy
