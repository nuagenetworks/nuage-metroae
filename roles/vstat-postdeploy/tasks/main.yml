---
- name: Register pid of elasticsearch process
  stat: path=/var/run/elasticsearch/elasticsearch.pid
  register: pid

- name: Check if pid of elasticsearch exists
  fail: msg="Elasticsearch process is not started. PID not found!"
  when: pid.stat.exists == false

- name: Check status of elasticsearch process
  command: systemctl is-active elasticsearch
