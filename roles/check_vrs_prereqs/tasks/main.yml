---

- name: Retrieve Docker version
  command: "docker -v"
  ignore_errors: True
  register: docker_installed_version

- name: Check if docker is installed when libnetwork_install is True
  fail: msg="Libnetwork_install is True but Docker is not installed"
  when: (docker_installed_version | failed) 
...
