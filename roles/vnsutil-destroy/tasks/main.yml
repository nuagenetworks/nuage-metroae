---
- include: kvm.yml
  when: target_server_type == "kvm"
  tags:
    - vnsutil
    - vnsutil-destroy

- include: heat.yml
  when: target_server_type == "heat"
  tags:
    - vnsutil
    - heat
    - vnsutil-destroy

- include: vcenter.yml
  when: target_server_type == "vcenter"
  tags:
    - vnsutil
    - vnsutil-destroy
