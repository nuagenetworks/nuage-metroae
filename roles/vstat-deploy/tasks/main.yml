---
- include: non_heat.yml
  when: ("{{ target_server_type }}" != "heat")
  tags:
    - vstat
    - vstat-deploy

- include: heat.yml
  when: ("{{ target_server_type }}" == "heat")
  tags:
    - vstat
    - heat
    - vstat-deploy
