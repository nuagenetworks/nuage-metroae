---
- include: non_heat.yml
  when: ("{{ target_server_type }}" != "heat")
  tags:
    - vstat
    - vstat-deploy

- include: vcenter.yml
  when: ("{{ target_server_type }}" == "vcenter")
  tags:
    - vstat
    - vstat-deploy

- include: heat.yml
  when: ("{{ target_server_type }}" == "heat")
  tags:
    - vstat
    - heat
    - vstat-deploy
