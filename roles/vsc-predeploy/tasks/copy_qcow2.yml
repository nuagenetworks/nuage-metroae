- name: Copy the VM qcow2 image to virt images diectory
  copy: src={{ qcow2_path }}/{{ qcow2_file_name }}
        dest={{ images_path }}/{{ vm_name }}/{{ inventory_hostname }}.qcow2
        owner={{ libvirt.user }}
        group={{ libvirt.group }}