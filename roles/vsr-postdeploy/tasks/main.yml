- name: Get config  
  local_action:
    module: netconf_config
    host: dcgw1
    hostkey_verify: no
    username: netops
    password: Net0ps
    save: yes
    xml: "{{ lookup('file', 'getconfig.xml') }}" 



    
