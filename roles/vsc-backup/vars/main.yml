vsc_creds:
  host: "{{ interfaces.mgmt.ip }}"
  username: admin
  password: admin
  transport: cli
expected_xmpp_server_state: Functional
transfer_folder: "scp -r admin@{{ interfaces.mgmt.ip }}:{{ backup_folder}} /tmp/vsc/{{ backup_folder }}"
