vsc_creds:
  host: "{{ mgmt_ip }}"
  username: "{{ vsc_user }}"
  password: "{{ vsc_password }}"
  transport: cli
  timeout: 180  # 3min
expected_xmpp_server_state: Functional
vsc_scp_backup_folder: "scp -r admin@{{ mgmt_ip }}:{{ backup_folder}} /tmp/{{ backup_folder }}"
vsc_scp_timeout: 360
