vsc_creds:
  host: "{{ mgmt_ip }}"
  username: "{{ vsc_user }}"
  password: "{{ vsc_password }}"
  transport: cli
expected_xmpp_server_state: Functional
transfer_folder: "scp -r admin@{{ mgmt_ip }}:{{ backup_folder}} /tmp/vsc/{{ backup_folder }}"
