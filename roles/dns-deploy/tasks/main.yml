---
- include: kvm.yml
  when: target_server_type == "kvm"
  tags:
    - dns
    - dns-deploy

- include: heat.yml
  when: target_server_type == "heat"
  tags:
    - dns
    - heat
    - dns-destroy

