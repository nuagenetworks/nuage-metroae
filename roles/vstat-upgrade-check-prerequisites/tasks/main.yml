- include: in_place_upgrade.yml
  remote_user: root
  when: in_place_upgrade

- name: Backup elasticsearch data
  include_role:
    name: vstat-data-backup
  vars:
    nuage_upgrade: true
  when: not in_place_upgrade
