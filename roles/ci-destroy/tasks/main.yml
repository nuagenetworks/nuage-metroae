---
- include: kvm.yml
  when: target_server_type == "kvm"
  tags:
    - ci
    - ci-destroy

- include: heat.yml
  when: target_server_type == "heat"
  tags:
    - ci
    - heat
    - ci-destroy
  run_once: true
