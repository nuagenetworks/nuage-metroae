---
- include: kvm.yml
  when: target_server_type == "kvm"
  tags:
    - dns
    - dns-postdeploy

- include: heat.yml
  when: target_server_type == "heat"
  tags:
    - dns
    - heat
    - dns-postdeploy

