---
- name: Destroy VSTAT AWS Stack
  delegate_to: localhost
  cloudformation:
    stack_name: "{{ vmname | replace('.', '-') }}"
    state: "absent"
    region: "{{ aws_region }}"
  register: rc_aws_cfn_nsgami
