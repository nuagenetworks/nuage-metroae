---
# Feature flag for dockermon post-deploy checks
#dockermon_postdeploy: true
- name: Set docker_check
  set_fact: docker_check={{dockermon_install is defined}}

dockermon_postdeploy: {{ dockermon_check }}
