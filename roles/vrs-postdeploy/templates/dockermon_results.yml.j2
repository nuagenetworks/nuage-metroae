---
# Temporary file
#
# This file contains outputs that are for dockermon verification commands

docker_pid: "{{ docker_pid_value.stdout }}"
docker_status: "{{ docker_status_result.stdout }}"
dockermon_pid: "{{ dockermon_pid_value.stdout }}"
dockermon_status: "{{ dockermon_status_result.stdout }}"
process_docker_status: "{{ ps_docker_result.stdout  | replace('\n','||') }}"
docker_logs: '{{ docker_mon_logs.stdout | replace('\n','||') }}'
