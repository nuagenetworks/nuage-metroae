---
- include: kvm.yml
  when: ("{{ target_server_type }}" == "kvm")
  tags:
    - compute
    - compute-predeploy

- include: heat.yml
  when: ("{{ target_server_type }}" == "heat")
  tags:
    - compute
    - compute-predeploy
