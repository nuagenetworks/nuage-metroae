---

- name: Destroy VSD AWS Stack
  delegate_to: localhost
  cloudformation:
    stack_name: "{{ inventory_hostname | replace('.', '-') }}"
    state: "absent"
    region: "{{ aws_region }}"
  register: rc_aws_cfn_nsgami

- debug: var=rc_aws_cfn_nsgami

