---
- hosts: 10.106.0.2
  gather_facts: no
  roles:
    - stacks-destroy
  vars:
    ansible_deployment_host: 10.106.0.2
    ansible_sudo_username: root
    os_auth:
      username: admin
      password: admin
      project_name: jen
      auth_url: "http://10.0.0.4:5000/v2.0" 
    os_env_file: /root/jenrc
    stack_older_than_hours: 4
    use_utc: True
    scan_only: False
