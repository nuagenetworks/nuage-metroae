---
- hosts: localhost
  gather_facts: yes
  vars:
    unzip_user: root
  pre_tasks:
    - name: Include build variable files
      include_vars: "{{ build_vars_file | default ('build_vars.yml') }}"
  roles:
    - nuage-unzip
