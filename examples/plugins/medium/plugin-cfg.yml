plugin_name: medium
description: |
  Plugin for creating custom vsd user
version: 1.0.0
required_metro_version: 4.1.0
schemas:
  - name: "newcredentials"
    is_list: no
    required: no
    encrypted: yes
hooks:
  - location: build
    role: common
    tasks: newcredentials-process-vars
