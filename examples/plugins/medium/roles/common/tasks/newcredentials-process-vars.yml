- name: Create vars for credentials
  block:

  - name: Create host_vars files for credentials
    include_tasks: write-host-files.yml
    vars:
      component_template: newcredentials
      component_hostname: newcredentials
  when:
    - newcredentials is defined and newcredentials|length > 0
