###############################################################################
# Demo VMs
#
# Specify configuration for the DemoVMs plugin.
#

#
# DemoVM 1
#
-
    # < Hostname >
    #
    hostname: "demovm1.example.met"

    # < Management IP address >
    #
    mgmt_ip: 192.168.1.141

    # < Management IP prefix length >
    #
    mgmt_ip_prefix: 24

    # < Management IP gateway >
    #
    mgmt_gateway: 192.168.1.1

    # < VM name >
    # Used to identify VM on the hypervisor/vcenter.
    #
    vmname: demovm1

    # < Target Server type >
    # (kvm, vcenter, heat, aws)
    #
    target_server_type: "kvm"

    # < Target Server hostname >
    #
    target_server: "127.0.0.1"

    # < Management Bridge interface >
    #
    # mgmt_bridge: (global Bridge interface)

    # < DEMOVM Data FQDN >
    #
    data_fqdn: "demovm1.data.example.met"

    # < DEMOVM Data IP address >
    #
    data_ip: "192.168.123.241"

    # < Data Netmask >
    #
    data_netmask: "255.255.255.0"

    # < Data Subnet >
    # Only required for DHCP bootstrap support
    #
    data_subnet: "192.168.123.0"

    # < Gateway address for NSGvs >
    # Gateway address to be provided by DHCP for NSGvs.
    #
    nsgv_gateway: "192.168.123.1"

    # < vCenter Datacenter >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datacenter: (global vCenter datacenter)

    # < vCenter Cluster >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_cluster: (global vCenter cluster)

    # < vCenter Datastore >
    # Only applicable for vcenter server type and defaults to global settings
    #
    # vcenter_datastore: (global vCenter datastore)

    # < AWS Region >
    # Only applicable for AWS deployments
    #
    # aws_region: ""

    # < AWS AMI ID >
    # AMI ID for AWS instance
    #
    # aws_ami_id: ""

    # < AWS Instance Type >
    # Instance Type of the AWS Instance
    #
    # aws_instance_type: ""

    # < AWS Keypair Name >
    # Name of the Keypair used to connect to VPC
    #
    # aws_key_name: ""

    # < AWS Management ENI ID >
    # ENI ID for DEMOVM Instance on Management Subnetwork
    #
    # aws_mgmt_eni: ""

    # < AWS Data ENI ID >
    # ENI ID for DEMOVM on Data Interface
    #
    # aws_data_eni: ""


    # < KVM cpuset information >
    #
    # cpuset: [ 15, 16 ]
    
