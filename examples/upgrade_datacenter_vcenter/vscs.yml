###############################################################################
# Virtualized Services Controller
#
# Specify configuration for Nuage VSCs.
#
# Automatically generated by script.
#



#
# VSC 1
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc1.company.ecom"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "192.168.110.25"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "192.168.110.1"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface. Define as empty list if no static routes are to be configured.
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "192.168.110.20"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    # < List of control IP VPRNs >
    # Comma Seperated list of control IP VPRNs. Each item in list is of format VLAN_ID:Control_IP_ADDR/NETMASK_LENGTH:NEXT_HOP . Control IP address, netmask length and next hop are optional
    #
    # ctrl_ip_vprn_list: []

    #####################

    ##### System info

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "192.168.100.20"

    # < Router ID >
    # The router ID of this VSC in IPV4 address format. Required when system_ip is IPV6.
    #
    # router_id: (System IP)

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    # vmname: (Hostname)

    # < System Name >
    # Name of the system if other than hostname
    #
    # system_name: (Hostname)

    # < XMPP username >
    # Unique username used to identify this VSC in its XMPP connection with VSD
    #
    # xmpp_username: vsc1

    #################

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment where VMs will be instantiated. Use 'none' when skipping predeploy.
    # (kvm, vcenter, openstack, none) 
    #
    target_server_type: "vcenter"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server. When target_server_type is set to openstack, this property is unused and should be set to 0.0.0.0
    #
    target_server: "10.105.1.100"

    ###################

    ##### Bridges and Private IP

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    # mgmt_bridge: (global Bridge interface)

    # < Data Network Bridge >
    # Network Bridge used for the data path of a component or the Control interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM.
    #
    # data_bridge: (global Bridge interface)

    # < LOG prefix Name >
    # Log prefix name to pass in the vsc config
    #
    # log_prefix_name: ""

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    # vsd_fqdn: ""

    # < Private Management IP >
    # Private Management IP Address of VSC instances
    #
    # internal_mgmt_ip: ""

    # < Private Control IP Address >
    # Private Control IP Address of VSC Instances
    #
    # internal_ctrl_ip: ""

    # < Private Data Gateway IP Address >
    # Private Data Gateway IP Address of VSC Instances
    #
    # internal_data_gateway_ip: ""

    # < List of route reflector IPs >
    # List of route reflector IP addresses if present
    #
    # route_reflector_ip_list: []

    ############################

    ##### BGP Interface

    # < BGP Interface IP Address >
    # IP Address for Optional BGP Interface
    #
    # bgp_interface_ip_address: ""

    # < BGP Interface Prefix length >
    # Prefix length for the optional BGP interface
    #
    # bgp_interface_ip_prefix: 24

    # < BGP Interface VLAN ID >
    # VLAN ID for the optional BGP interface
    #
    # bgp_interface_vlan_id: 1000

    ###################

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    vcenter_datacenter: "DC"

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    vcenter_cluster: "Cluster-A"

    # < vCenter Host Reference >
    # Requires ovftool 4.3. Reference to the host on the vCenter cluster on which to deploy Nuage components
    #
    # vcenter_host_reference: (global vCenter Host Reference)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    vcenter_datastore: "datastoreA"

    # < vCenter VM Folder >
    # Optional path to a folder defined on vCenter where VM will be instantiated
    #
    # vcenter_vmfolder: (global vCenter VM folder)

    # < vCenter Hosts and Clusters Folder >
    # Optional path to a hosts and clusters folder defined on vCenter where VM will be instantiated
    #
    # vcenter_host_clusters_folder: ""

    ########################

    # < First External IP address >
    # The first External IP address of the controller
    #
    # external_ip1: ""

    # < First External Netmask Prefix Length >
    # First External network prefix length
    #
    # external_ip1_prefix: 0

    # < Second External IP address >
    # The second External IP address of the controller
    #
    # external_ip2: ""

    # < Second External Netmask Prefix Length >
    # Second External network prefix length
    #
    # external_ip2_prefix: 0

    # < Third External IP address >
    # The third External IP address of the controller
    #
    # external_ip3: ""

    # < Third External Netmask Prefix Length >
    # Third External network prefix length
    #
    # external_ip3_prefix: 0

    # < First External Port's Segmentation Id >
    # Segmentation id of first external port to be used during trunking
    #
    # segmentation_id1: 0

    # < Second External Port's Segmentation Id >
    # Segmentation id of first external port to be used during trunking
    #
    # segmentation_id2: 0

    # < Third External Port's Segmentation Id >
    # Segmentation id of third external port to be used during trunking
    #
    # segmentation_id3: 0

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### Certificates and credentials

    # < Ejabberd user id >
    # Ejabberd user id used to create the certificate
    #
    # ejabberd_id: ""

    # < Private key path >
    # Path to VSC certificate key pem file
    #
    # private_key_path: ""

    # < Certificate path >
    # Path to VSC certificate pem file
    #
    # certificate_path: ""

    # < CA certificate path >
    # Path to CA certificate pem file
    #
    # ca_certificate_path: ""

    # < XMPP domain >
    # XMPP domain used in custom certificates
    #
    # xmpp_domain: ""

    # < Credentials set name >
    # Name of the credentials set for the vsc
    #
    # credentials_set: ""

    ##################################

    # < Harden VSC >
    # Enables hardening configuration on VSC
    #
    # enable_hardening: True

    # < VSC deployed as VRR >
    # Deploys a VRR, for onprem only
    #
    # vrr_enabled: False

    # < Paths to VSC Config Files >
    # Paths to files that can be optionally applied for additional VSC configuration
    #
    # vsc_config_file_paths: []

    # < Override the configuration on VSC >
    # This will override the Metro Provided config on the VSC by the config provided in vsc_config_file_paths
    #
    # override_vsc_config: False

    #################


#
# VSC 2
#
-
    ##### Management Network

    # < Hostname >
    # Hostname of the VSC instance
    #
    hostname: "vsc2.company.ecom"

    # < Management IP address >
    # The BOF/Management IP address of the controller
    #
    mgmt_ip: "192.168.110.26"

    # < Management Network Prefix Length >
    # Management network prefix length
    #
    mgmt_ip_prefix: 24

    # < Management Network Gateway >
    # Gateway IP on the Management network
    #
    mgmt_gateway: "192.168.110.1"

    # < Management Network Static Routes >
    # Static Route list to be configured on the management/BOF interface. Define as empty list if no static routes are to be configured.
    #
    # mgmt_static_route_list: [ 0.0.0.0/1, 128.0.0.0/1 ]

    ########################

    ##### Control Network

    # < Control IP address >
    # The Control/Data IP address of the controller
    #
    ctrl_ip: "192.168.110.21"

    # < Control Netmask Prefix Length >
    # Control network prefix length
    #
    ctrl_ip_prefix: 24

    # < List of control IP VPRNs >
    # Comma Seperated list of control IP VPRNs. Each item in list is of format VLAN_ID:Control_IP_ADDR/NETMASK_LENGTH:NEXT_HOP . Control IP address, netmask length and next hop are optional
    #
    # ctrl_ip_vprn_list: []

    #####################

    ##### System info

    # < System IP address >
    # Required for BGP pairing with peers
    #
    system_ip: "192.168.100.21"

    # < Router ID >
    # The router ID of this VSC in IPV4 address format. Required when system_ip is IPV6.
    #
    # router_id: (System IP)

    # < VM name >
    # Name of the virtual machine on the Hypervisor/vCenter Server.
    #
    # vmname: (Hostname)

    # < System Name >
    # Name of the system if other than hostname
    #
    # system_name: (Hostname)

    # < XMPP username >
    # Unique username used to identify this VSC in its XMPP connection with VSD
    #
    # xmpp_username: vsc1

    #################

    ##### Target Server

    # < Target Server type >
    # Type of hypervisor environment where VMs will be instantiated. Use 'none' when skipping predeploy.
    # (kvm, vcenter, openstack, none) 
    #
    target_server_type: "vcenter"

    # < Target Server IP or FQDN >
    # Hostname or IP address of the hypervisor where VM  will be instantiated. In the case of deployment in a vCenter environment, this will be the FQDN of the vCenter Server. When target_server_type is set to openstack, this property is unused and should be set to 0.0.0.0
    #
    target_server: "10.105.1.101"

    ###################

    ##### Bridges and Private IP

    # < Management Network Bridge >
    # Network Bridge used for the management interface of a component or the BOF interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM. This field can be overridden by defining the management network bridge separately in the component configuration. Defaults to the global setting
    #
    # mgmt_bridge: (global Bridge interface)

    # < Data Network Bridge >
    # Network Bridge used for the data path of a component or the Control interface on VSC. This will be a Distributed Virtual PortGroup (DVPG) when deploying on vCenter or a Linux network bridge when deploying on KVM.
    #
    # data_bridge: (global Bridge interface)

    # < LOG prefix Name >
    # Log prefix name to pass in the vsc config
    #
    # log_prefix_name: ""

    # < VSD FQDN >
    # FQDN of the VSD or VSD cluster for this VSC
    #
    # vsd_fqdn: ""

    # < Private Management IP >
    # Private Management IP Address of VSC instances
    #
    # internal_mgmt_ip: ""

    # < Private Control IP Address >
    # Private Control IP Address of VSC Instances
    #
    # internal_ctrl_ip: ""

    # < Private Data Gateway IP Address >
    # Private Data Gateway IP Address of VSC Instances
    #
    # internal_data_gateway_ip: ""

    # < List of route reflector IPs >
    # List of route reflector IP addresses if present
    #
    # route_reflector_ip_list: []

    ############################

    ##### BGP Interface

    # < BGP Interface IP Address >
    # IP Address for Optional BGP Interface
    #
    # bgp_interface_ip_address: ""

    # < BGP Interface Prefix length >
    # Prefix length for the optional BGP interface
    #
    # bgp_interface_ip_prefix: 24

    # < BGP Interface VLAN ID >
    # VLAN ID for the optional BGP interface
    #
    # bgp_interface_vlan_id: 1000

    ###################

    ##### vCenter Parameters

    # < vCenter Datacenter Name >
    # Name of the vCenter Datacenter on which the VSC VM will be deployed. Defaults to the common vCenter Datacenter Name if not defined here.
    #
    vcenter_datacenter: "DC"

    # < vCenter Cluster Name >
    # Name of the vCenter Cluster on which the VSC VM will be deployed. Defaults to the common vCenter Cluster Name if not defined here.
    #
    vcenter_cluster: "Cluster-B"

    # < vCenter Host Reference >
    # Requires ovftool 4.3. Reference to the host on the vCenter cluster on which to deploy Nuage components
    #
    # vcenter_host_reference: (global vCenter Host Reference)

    # < vCenter Datastore Name >
    # Name of the vCenter Datastore on which the VSC VM will be deployed. Defaults to the common vCenter Datastore Name if not defined here.
    #
    vcenter_datastore: "datastoreB"

    # < vCenter VM Folder >
    # Optional path to a folder defined on vCenter where VM will be instantiated
    #
    # vcenter_vmfolder: (global vCenter VM folder)

    # < vCenter Hosts and Clusters Folder >
    # Optional path to a hosts and clusters folder defined on vCenter where VM will be instantiated
    #
    # vcenter_host_clusters_folder: ""

    ########################

    # < First External IP address >
    # The first External IP address of the controller
    #
    # external_ip1: ""

    # < First External Netmask Prefix Length >
    # First External network prefix length
    #
    # external_ip1_prefix: 0

    # < Second External IP address >
    # The second External IP address of the controller
    #
    # external_ip2: ""

    # < Second External Netmask Prefix Length >
    # Second External network prefix length
    #
    # external_ip2_prefix: 0

    # < Third External IP address >
    # The third External IP address of the controller
    #
    # external_ip3: ""

    # < Third External Netmask Prefix Length >
    # Third External network prefix length
    #
    # external_ip3_prefix: 0

    # < First External Port's Segmentation Id >
    # Segmentation id of first external port to be used during trunking
    #
    # segmentation_id1: 0

    # < Second External Port's Segmentation Id >
    # Segmentation id of first external port to be used during trunking
    #
    # segmentation_id2: 0

    # < Third External Port's Segmentation Id >
    # Segmentation id of third external port to be used during trunking
    #
    # segmentation_id3: 0

    ##### Health Parameters

    # < Expected number of BGP neighbors >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_bgp_peers: 0

    # < Expected number of vswitches >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vswitches: 0

    # < Expected number of host vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_host_vports: 0

    # < Expected number of VM vports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_vm_vports: 0

    # < Expected number of gateway ports >
    # Used in postdeploy and health workflows as expected values if non-zero
    #
    # expected_num_gateway_ports: 0

    #######################

    ##### Certificates and credentials

    # < Ejabberd user id >
    # Ejabberd user id used to create the certificate
    #
    # ejabberd_id: ""

    # < Private key path >
    # Path to VSC certificate key pem file
    #
    # private_key_path: ""

    # < Certificate path >
    # Path to VSC certificate pem file
    #
    # certificate_path: ""

    # < CA certificate path >
    # Path to CA certificate pem file
    #
    # ca_certificate_path: ""

    # < XMPP domain >
    # XMPP domain used in custom certificates
    #
    # xmpp_domain: ""

    # < Credentials set name >
    # Name of the credentials set for the vsc
    #
    # credentials_set: ""

    ##################################

    # < Harden VSC >
    # Enables hardening configuration on VSC
    #
    # enable_hardening: True

    # < VSC deployed as VRR >
    # Deploys a VRR, for onprem only
    #
    # vrr_enabled: False

    # < Paths to VSC Config Files >
    # Paths to files that can be optionally applied for additional VSC configuration
    #
    # vsc_config_file_paths: []

    # < Override the configuration on VSC >
    # This will override the Metro Provided config on the VSC by the config provided in vsc_config_file_paths
    #
    # override_vsc_config: False

    #################



