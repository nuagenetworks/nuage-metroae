Schema name: deployment
{'deployment_description': 'Install all SD-WAN components (VSD, VSC, VSTAT, NSG and VNSUtil) on KVM', 'deployment_name': 'Install SD-WAN components on KVM'}
Schema name: deployment
{'deployment_description': 'Install all SD-WAN components (VSD, VSC, VSTAT, NSG and VNSUtil) on KVM', 'deployment_name': 'Install SD-WAN components on KVM'}
###############################################################################
# Deployment
#
# Nuage VSP Deployment
#
# Automatically generated by script.
#

# < Name >
# The name of the deployment.  This will be used as the the deployment directory name.  No special characters allowed.
#
deployment_name: "Install SD-WAN components on KVM"

# < Description >
# A suitable description of the deployment
#
deployment_description: "Install all SD-WAN components (VSD, VSC, VSTAT, NSG and VNSUtil) on KVM"
