Schema name: deployment
{'deployment_description': 'Upgrade one VSC with non-root user on KVM', 'deployment_name': 'Upgrade a single component(VSC) on KVM'}
Schema name: deployment
{'deployment_description': 'Upgrade one VSC with non-root user on KVM', 'deployment_name': 'Upgrade a single component(VSC) on KVM'}
###############################################################################
# Deployment
#
# Nuage VSP Deployment
#
# Automatically generated by script.
#

# < Name >
# The name of the deployment.  This will be used as the the deployment directory name.  No special characters allowed.
#
deployment_name: "Upgrade a single component(VSC) on KVM"

# < Description >
# A suitable description of the deployment
#
deployment_description: "Upgrade one VSC with non-root user on KVM"
