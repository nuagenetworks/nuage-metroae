---
- include: kvm.yml
  when: target_server_type == "kvm"
  tags:
    - vstat
    - vstat-predeploy

- include: vcenter.yml
  when: target_server_type == "vcenter"
  tags:
    - vstat
    - vstat-predeploy
