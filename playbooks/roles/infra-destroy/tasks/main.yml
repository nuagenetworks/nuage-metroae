---
- include: kvm.yml
  when: target_server_type | match("kvm")
  tags:
    - infra
    - infra-destroy

- include: heat.yml
  when: target_server_type | match("heat")
  tags:
    - infra
    - heat
    - infra-destroy
