---
# Post Deploy specific Variables
vsc_creds:
  host: "{{ mgmt_ip }}"
  username: "{{ vsc_user|default('admin') }}"
  password: "{{ vsc_password|default('admin') }}"
  timeout: 180
# Timeout in seconds
retry_timeout: 300
# NTP sync params
ntp_sync_retries: 90
ntp_sync_delay: 10
