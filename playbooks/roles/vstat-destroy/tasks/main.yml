---
- include: kvm.yml
  when: target_server_type == "kvm"
  tags:
    - vstat
    - vstat-destroy

- include: heat.yml
  when: target_server_type == "heat"
  tags:
    - vstat
    - heat
    - vstat-destroy

- include: vcenter.yml
  when: target_server_type == "vcenter"
  tags:
    - vstat
    - vstat-destroy
