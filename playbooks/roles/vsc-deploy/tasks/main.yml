---
- include: kvm.yml
  when: ("{{ target_server_type }}" == "kvm")
  tags:
    - vsc
    - vsc-deploy

- include: heat.yml
  when: ("{{ target_server_type }}" == "heat")
  tags:
    - vsc
    - heat
    - vsc-deploy

- include: vcenter.yml
  when: ("{{ target_server_type }}" == "vcenter")
  tags:
    - vsc
    - vsc-deploy
