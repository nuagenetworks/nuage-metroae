- hosts: vrss
  gather_facts: no
  roles:
    - vrs-deploy
  become: yes
  remote_user: "{{ vrs_remote_user | default('root') }}"

- hosts: vcins
  gather_facts: no
  roles:
    - vcin-vrs-upgrade
  become: yes
  connection: local


