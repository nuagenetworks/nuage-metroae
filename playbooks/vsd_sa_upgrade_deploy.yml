---
- hosts: vsds
  gather_facts: no
  roles:
    - vsd-upgrade-prepare-for-deploy

- hosts: vsds
  gather_facts: no
  roles:
    - vsd-deploy
  become: "{{ 'no' if vsd_username == 'root' else 'yes' }}"
