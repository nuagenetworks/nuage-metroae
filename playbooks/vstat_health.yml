---
- hosts: vsds
  gather_facts: no
  roles:
    - vstat-vsd-health

- hosts: vscs
  gather_facts: no
  roles:
    - vstat-vsc-health

- hosts: vrss
  gather_facts: no
  roles:
    - vstat-vrs-health
  remote_user: "{{ vrs_remote_user | default('root') }}"

