---
- hosts: vstats
  roles:
    - vstat-postdeploy
  become: "{{ 'no' if vstat_default_username == 'root' else 'yes' }}"
  remote_user: "{{ vstat_default_username }}"
  gather_facts: no
  vars:
    ansible_become_pass: "{{ vstat_default_password }}"
