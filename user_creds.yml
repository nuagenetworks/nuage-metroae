# Sample user credential settings for VSP
# Replace the values, below, with those that match your environment
#
# VSD API/Architect auth params

vsd_auth:
    username: csproot
    password: csproot
    enterprise: csp
    api_url: https://{{ vsd_fqdn_global | default('vsd.example.com') }}:8443

vcin_auth:
  api_username: csproot
  api_password: csproot
  api_enterprise: csp


#
# VSC auth params
# TODO:
#  - could be expanded to each VSC
#  - could be put in a dict object, similar to vsd_auth

vsc_user: admin
vsc_password: admin

vstat_user: root
vstat_password: Alcateldc
