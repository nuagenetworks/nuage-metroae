---
- hosts: vscs
  gather_facts: no
  vars:
    vsc_creds:
      host: "{{ interfaces.mgmt.ip }}"
      username: admin
      password: admin
      transport: cli
    expected_num_bgp_peers: 2
    expected_xmpp_server_state: Functional
    expected_num_vswitches: 1
    expected_num_host_vports: 0
    expected_num_vm_vports: 0
  roles:
    - vsc-health
