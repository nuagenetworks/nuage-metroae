---
- hosts: vscs
  gather_facts: no
  vars:
    vsc_creds:
      host: "{{ interfaces.mgmt.ip }}"
      username: admin
      password: admin
      transport: cli
    expected_num_bgp_peers: 2
  roles:
    - vsc-health
